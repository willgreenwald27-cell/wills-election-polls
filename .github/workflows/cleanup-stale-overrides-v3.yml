name: Cleanup stale overrides v3
on:
  push:
    paths:
      - '.github/workflows/cleanup-stale-overrides-v3.yml'
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Stale override cleanup retired"
